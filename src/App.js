import React from 'react';
import Graph from './graph';
import SelectedJob from './selected-job';

const App = props => {
  return (
    <div>
      <Graph />
      <SelectedJob />
    </div>
  );
}

export default App;
